export interface AvailabilityItem {
    hour: number;
    availability: boolean;
}

export interface Car {
    id: string;
    nome: string;
    desc: string;
    img: string;
    km: number;
    local: string;
    ano: string;
    preco: string;
}